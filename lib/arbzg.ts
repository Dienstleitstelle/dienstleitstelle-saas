/**
 * Diese Datei ist veraltet. Die Pruef-Logik liegt jetzt in lib/regeln.ts.
 * Die Datei bleibt leer fuer eventuelle alte Imports und kann entfernt werden,
 * sobald sicher ist, dass nichts mehr referenziert.
 */
export {};
